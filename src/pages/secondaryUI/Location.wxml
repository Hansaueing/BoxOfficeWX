<view class="page-section page-section-gap">
    <map
      id="myMap"
      style="width: 100%; height: 1500rpx;"
      latitude="{{latitude}}"
      longitude="{{longitude}}"
      markers="{{markers}}"
      scale = "15"
      show-location
    ></map>
  </view>